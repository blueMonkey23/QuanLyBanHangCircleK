function revenueReport(req, res) {
  res.json({ message: 'Revenue report (stub)' });
}

function topProductsReport(req, res) {
  res.json({ message: 'Top products report (stub)' });
}

module.exports = {
  revenueReport,
  topProductsReport,
};
